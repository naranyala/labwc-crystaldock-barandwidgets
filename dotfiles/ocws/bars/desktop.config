#Api2
# bars/desktop.config — Desktop layer for floating widgets
# Used by: dual, desktop modes

bar "desktop:bottom" {
  edge = "bottom"
  layer = "background"
  mirror = "*"
  exclusive_zone = "-1"
  size = 0

  # --- Floating desktop widgets ---
  include("widget-sets/desktop.set")
}
