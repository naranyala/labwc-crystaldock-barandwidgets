#Api2
# modes/desktop.config — Desktop layer for floating widgets
# Background layer with clock, weather, sysmon widgets

bar "desktop:bottom" {
  edge = "bottom"
  layer = "background"
  mirror = "*"
  exclusive_zone = -1
  size = 0

  # --- Floating desktop widgets ---
  include("widget-sets/desktop.set")
}
