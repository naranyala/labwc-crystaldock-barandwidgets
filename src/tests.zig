const std = @import("std");
const testing = std.testing;

test "ocws dummy test" {
    // This is a placeholder for native C function unit testing
    // to be done by importing the C functions once they are separated
    // from main() into static libraries.
    try testing.expect(true);
}
